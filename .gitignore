# Model weights and adapters (too large for git)
checkpoints/

# llama.cpp clone + pre-built binaries (too large for git)
tools/
*.safetensors
*.bin
*.pt

# Evaluation raw results and HuggingFace metadata
evaluation/eval_results/
evaluation/results/

# Real audio recordings: personal data (voices, demographics, IP in metadata).
# NEVER commit to the public repo. Only aggregate scores under real_audio_results/.
evaluation/real_audio/
evaluation/__huggingface_repos__.json
__huggingface_repos__.json

# Generated data
data/synthetic/sme_all.jsonl

# Recordings from demo (audio files)
recordings/

# Calendar state (modified by mock bookings at runtime)
# Keep data/calendar.json in git as the default seed — but not the runtime state
data/*.bak

# Private project memory (never commit)
CLAUDE.md
docs/

# Team learning pack (kept on OneDrive, not in the public repo)
team-pack/

# Python cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
*.egg-info/

# Virtual env
venv/
.venv/

# Claude Code local config
.claude/

# Secrets
.env
*.env

# Piper TTS binary and voice model (large files, not tracked)
piper/

# OS files
.DS_Store
Thumbs.db

# Non-code documents (word docs, assessment briefs, uni files)
docs/
*.docx
*.pdf
data/metrics.db

# Caller voicemail messages (personal data, local only)
data/messages.jsonl
