# Model weights and adapters (too large for git)
checkpoints/
*.safetensors
*.bin
*.pt

# Evaluation raw results and HuggingFace metadata
evaluation/eval_results/
evaluation/results/
evaluation/__huggingface_repos__.json
__huggingface_repos__.json

# Generated data
data/synthetic/sme_all.jsonl

# Recordings from demo (audio files)
recordings/

# Calendar state (modified by mock bookings at runtime)
# Keep data/calendar.json in git as the default seed — but not the runtime state
data/*.bak

# Private project memory (never commit)
CLAUDE.md
docs/

# Python cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
*.egg-info/

# Virtual env
venv/
.venv/

# Secrets
.env
*.env

# Piper TTS binary and voice model (large files, not tracked)
piper/

# OS files
.DS_Store
Thumbs.db

# Non-code documents (word docs, assessment briefs, uni files)
docs/
*.docx
*.pdf